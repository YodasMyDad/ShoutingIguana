<UserControl x:Class="ShoutingIguana.Views.FindingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sdk="clr-namespace:ShoutingIguana.PluginSdk;assembly=ShoutingIguana.PluginSdk"
             Background="{StaticResource BackgroundBrush}">
    
    <UserControl.InputBindings>
        <KeyBinding Key="C" Modifiers="Control" Command="{Binding SelectedTab.CopyUrlCommand}"/>
        <KeyBinding Key="C" Modifiers="Control+Shift" Command="{Binding SelectedTab.CopyMessageCommand}"/>
        <KeyBinding Key="Enter" Modifiers="Control" Command="{Binding SelectedTab.OpenInBrowserCommand}"/>
    </UserControl.InputBindings>
    
    <UserControl.Resources>
        <!-- Severity Badge Selector -->
        <Style x:Key="SeverityBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Error}">
                    <Setter Property="Background" Value="#FFEBEE"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Warning}">
                    <Setter Property="Background" Value="#FFF8E1"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Info}">
                    <Setter Property="Background" Value="#E3F2FD"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="SeverityTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Error}">
                    <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Warning}">
                    <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Info}">
                    <Setter Property="Foreground" Value="{StaticResource InfoBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="{StaticResource SpacingL}" KeyboardNavigation.TabNavigation="Local">
            <Grid.RenderTransform>
                <TranslateTransform/>
            </Grid.RenderTransform>
            <Grid.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                             From="20" To="0" Duration="0:0:0.4"
                                             DecelerationRatio="0.7"/>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0" To="1" Duration="0:0:0.4"
                                             DecelerationRatio="0.7"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Grid.Triggers>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*" MinHeight="600"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*" MinHeight="220"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="8"
                Padding="24"
                Margin="0,0,0,24"
                SnapsToDevicePixels="True">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="2" Opacity="0.06"/>
            </Border.Effect>
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource InfoBrush}"
                            Width="44" Height="44"
                            CornerRadius="8"
                            Margin="0,0,16,0">
                        <TextBlock Text="{StaticResource Icon.Search}"
                                   Style="{StaticResource Icon}"
                                   FontSize="22"
                                   Foreground="White"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Findings"
                                   Style="{StaticResource DisplayTextStyle}"
                                   FontSize="24"
                                   Margin="0"
                                   AutomationProperties.HeadingLevel="Level1"
                                   AutomationProperties.LiveSetting="Off"
                                   AutomationProperties.AutomationId="FindingsPageHeading"/>
                        <TextBlock Text="Issues and insights discovered during crawl"
                                   Style="{StaticResource BodyTextStyle}"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel Style="{StaticResource ToolbarPanel}" 
                            AutomationProperties.Name="Findings Toolbar"
                            KeyboardNavigation.TabNavigation="Local">
                    <Button Command="{Binding RefreshCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,6,0"
                            TabIndex="0"
                            AutomationProperties.Name="Refresh Findings"
                            ToolTip="Refresh findings from database (F5)">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{StaticResource Icon.Refresh}"
                                       Style="{StaticResource Icon}"
                                       Width="16" 
                                       Height="16"
                                       Margin="0,0,6,0"/>
                            <TextBlock Text="Refresh" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ExportToCsvCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,6,0"
                            TabIndex="1"
                            AutomationProperties.Name="Export to CSV"
                            ToolTip="Export findings to CSV file (Ctrl+Shift+E)">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{StaticResource Icon.FileCsv}"
                                       Style="{StaticResource Icon}"
                                       Width="16" 
                                       Height="16"
                                       Margin="0,0,6,0"/>
                            <TextBlock Text="CSV" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ExportToExcelCommand}"
                            Style="{StaticResource PrimaryButton}"
                            TabIndex="2"
                            AutomationProperties.Name="Export to Excel"
                            ToolTip="Export findings to Excel workbook (Ctrl+E)">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{StaticResource Icon.FileExcel}"
                                       Style="{StaticResource Icon}"
                                       Width="16" 
                                       Height="16"
                                       Margin="0,0,6,0"/>
                            <TextBlock Text="Excel" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tabbed Findings View -->
        <Border Grid.Row="1" 
                Style="{StaticResource Card}"
                Padding="{StaticResource SpacingXs}">
            <Grid>
                <!-- Empty State (visible when no tabs) -->
                <StackPanel Style="{StaticResource EmptyStateContainer}"
                            Visibility="{Binding Tabs.Count, Converter={StaticResource InverseInt32ToVisibilityConverter}}"
                            x:Name="EmptyStatePanel">
                    <TextBlock Text="{StaticResource Icon.Search}"
                               Style="{StaticResource EmptyStateIcon}"/>
                    <TextBlock Text="No Findings Yet"
                               Style="{StaticResource EmptyStateTitle}"/>
                    <TextBlock Text="Start a crawl to begin analyzing your website. Findings from all enabled plugins will appear here."
                               Style="{StaticResource EmptyStateMessage}"/>
                </StackPanel>
                
                <!-- Tabbed Content (visible when tabs exist) -->
                <TabControl ItemsSource="{Binding Tabs}" 
                            SelectedItem="{Binding SelectedTab}"
                            Style="{StaticResource StandardTabControl}"
                            MinHeight="500"
                            KeyboardNavigation.TabNavigation="Local"
                            Visibility="{Binding Tabs.Count, Converter={StaticResource Int32ToVisibilityConverter}}">
                <TabControl.ItemContainerStyle>
                    <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                        <Setter Property="Height" Value="48"/>
                    </Style>
                </TabControl.ItemContainerStyle>
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="12,0">
                            <TextBlock Text="{Binding TabHeader}" 
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"
                                       FontWeight="Medium"/>
                            <Border Background="{StaticResource ErrorBrush}" 
                                    CornerRadius="10" 
                                    Padding="6,2"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding ErrorCount, Converter={StaticResource Int32ToVisibilityConverter}}">
                                <TextBlock Text="{Binding ErrorCount}" 
                                           Foreground="White" 
                                           FontSize="10"
                                           FontWeight="Bold"/>
                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <Grid Margin="16" MinHeight="400">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*" MinHeight="300"/>
                            </Grid.RowDefinitions>

                            <!-- Filters -->
                            <Border Background="{StaticResource SurfaceAltBrush}"
                                    CornerRadius="6"
                                    Padding="16"
                                    Margin="0,0,0,16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Severity Filter -->
                                    <StackPanel Grid.Column="0" Margin="0">
                                        <TextBlock Text="Severity" 
                                                   Style="{StaticResource FormFieldLabel}"
                                                   Margin="0,0,0,8"
                                                   AutomationProperties.AutomationId="SeverityFilterLabel"/>
                                        <ComboBox Width="200"
                                                  MaxDropDownHeight="300"
                                                  SelectedValue="{Binding SelectedSeverity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource ModernComboBox}"
                                                  TabIndex="3"
                                                  AutomationProperties.Name="Filter by Severity"
                                                  ToolTip="Filter findings by severity level">
                                            <ComboBoxItem Tag="{x:Null}" MaxWidth="180">
                                                <TextBlock Text="All Severities"/>
                                            </ComboBoxItem>
                                            <ComboBoxItem Tag="{x:Static sdk:Severity.Error}" MaxWidth="180">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{StaticResource Icon.Error}" 
                                                               Style="{StaticResource Icon}"
                                                               FontSize="14"
                                                               Foreground="{StaticResource ErrorBrush}"
                                                               Margin="0,0,8,0"/>
                                                    <TextBlock Text="Error" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </ComboBoxItem>
                                            <ComboBoxItem Tag="{x:Static sdk:Severity.Warning}" MaxWidth="180">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{StaticResource Icon.Warning}" 
                                                               Style="{StaticResource Icon}"
                                                               FontSize="14"
                                                               Foreground="{StaticResource WarningBrush}"
                                                               Margin="0,0,8,0"/>
                                                    <TextBlock Text="Warning" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </ComboBoxItem>
                                            <ComboBoxItem Tag="{x:Static sdk:Severity.Info}" MaxWidth="180">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{StaticResource Icon.Info}" 
                                                               Style="{StaticResource Icon}"
                                                               FontSize="14"
                                                               Foreground="{StaticResource InfoBrush}"
                                                               Margin="0,0,8,0"/>
                                                    <TextBlock Text="Info" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </ComboBoxItem>
                                        </ComboBox>
                                    </StackPanel>
                                    
                                    <!-- Search Box -->
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Search" 
                                                   Style="{StaticResource FormFieldLabel}"
                                                   Margin="0,0,0,8"
                                                   AutomationProperties.AutomationId="SearchFilterLabel"/>
                                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource ModernTextBox}"
                                                 TabIndex="4"
                                                 AutomationProperties.Name="Search Findings"
                                                 AutomationProperties.HelpText="Search findings by message, code, or URL"
                                                 ToolTip="Search findings by message, code, or URL">
                                            <TextBox.Tag>Type to search...</TextBox.Tag>
                                        </TextBox>
                                    </StackPanel>
                                             
                                    <!-- Total Count Badge -->
                                    <Border Grid.Column="4"
                                            Background="{StaticResource PrimaryBrush}"
                                            CornerRadius="8"
                                            Padding="20,12"
                                            VerticalAlignment="Bottom"
                                            Height="40">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="Total: "
                                                       Foreground="White"
                                                       FontWeight="Medium"
                                                       FontSize="13"
                                                       VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding TotalCount, Mode=OneWay}" 
                                                       Foreground="White"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       VerticalAlignment="Center"
                                                       Margin="4,0,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>

                            <!-- Findings DataGrid -->
                            <Grid Grid.Row="1" MinHeight="300">
                                <!-- Skeleton Loader (shown while loading) -->
                                <ItemsControl Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                                              Margin="12">
                                    <ItemsControl.Items>
                                        <!-- 8 skeleton rows -->
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="120"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="200"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="280"/>
                                                <Rectangle Grid.Column="4" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="100"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="250"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="300"/>
                                                <Rectangle Grid.Column="4" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="130"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="180"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="290"/>
                                                <Rectangle Grid.Column="4" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="110"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="220"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="310"/>
                                                <Rectangle Grid.Column="4" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="125"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="190"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="270"/>
                                                <Rectangle Grid.Column="4" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="115"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="240"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="295"/>
                                                <Rectangle Grid.Column="4" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="105"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="210"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="285"/>
                                                <Rectangle Grid.Column="4" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="135"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="230"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="275"/>
                                                <Rectangle Grid.Column="4" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                    </ItemsControl.Items>
                                </ItemsControl>
                                
                                <!-- Actual DataGrid (hidden while loading) -->
                                <DataGrid Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                          ItemsSource="{Binding FilteredFindings}"
                                          SelectedItem="{Binding SelectedFinding, Mode=TwoWay}"
                                          Style="{StaticResource ModernDataGrid}"
                                          MinHeight="250"
                                          TabIndex="5"
                                          AutomationProperties.Name="Findings List"
                                          AutomationProperties.HelpText="List of findings discovered during the crawl. Select a row to view details below."
                                          AutomationProperties.LiveSetting="Polite">
                                <DataGrid.ContextMenu>
                                    <ContextMenu AutomationProperties.Name="Findings Context Menu">
                                        <MenuItem Header="Copy URL" 
                                                  Command="{Binding CopyUrlCommand}"
                                                  InputGestureText="Ctrl+C"
                                                  AutomationProperties.Name="Copy URL to Clipboard"
                                                  AutomationProperties.HelpText="Copy the selected finding's URL to clipboard (Ctrl+C)">
                                            <MenuItem.Icon>
                                                <TextBlock Text="{StaticResource Icon.Copy}" Style="{StaticResource Icon}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="Copy Message" 
                                                  Command="{Binding CopyMessageCommand}"
                                                  InputGestureText="Ctrl+Shift+C"
                                                  AutomationProperties.Name="Copy Message to Clipboard"
                                                  AutomationProperties.HelpText="Copy the selected finding's message to clipboard (Ctrl+Shift+C)">
                                            <MenuItem.Icon>
                                                <TextBlock Text="{StaticResource Icon.Copy}" Style="{StaticResource Icon}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem Header="Open in Browser" 
                                                  Command="{Binding OpenInBrowserCommand}"
                                                  InputGestureText="Ctrl+Enter"
                                                  AutomationProperties.Name="Open URL in Default Browser"
                                                  AutomationProperties.HelpText="Opens the selected finding's URL in your default web browser (Ctrl+Enter)">
                                            <MenuItem.Icon>
                                                <TextBlock Text="{StaticResource Icon.OpenInNew}" Style="{StaticResource Icon}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </DataGrid.ContextMenu>
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="Severity" Width="110">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Style="{StaticResource SeverityBadgeStyle}">
                                                    <TextBlock Text="{Binding Severity}" 
                                                               Style="{StaticResource SeverityTextStyle}"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Code" 
                                                        Binding="{Binding Code}" 
                                                        Width="150"
                                                        FontFamily="Consolas"
                                                        FontSize="13"/>
                                    <DataGridTextColumn Header="Message" 
                                                        Binding="{Binding Message}" 
                                                        Width="*"
                                                        MinWidth="200"/>
                                    <DataGridTextColumn Header="URL" 
                                                        Binding="{Binding Url.Address}" 
                                                        Width="350"
                                                        FontFamily="Consolas"
                                                        FontSize="12"/>
                                    <DataGridTextColumn Header="Date" 
                                                        Binding="{Binding CreatedUtc, StringFormat='yyyy-MM-dd HH:mm:ss'}" 
                                                        Width="160"
                                                        FontFamily="Consolas"
                                                        FontSize="12"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </TabControl.ContentTemplate>
                </TabControl>
            </Grid>
        </Border>

        <!-- Splitter -->
        <GridSplitter Grid.Row="2" 
                      Height="8" 
                      HorizontalAlignment="Stretch" 
                      Style="{StaticResource ModernGridSplitter}"
                      Cursor="SizeNS"
                      Margin="0,8"
                      AutomationProperties.Name="Resize Findings and Details Panels"/>

        <!-- Details Panel -->
        <Border Grid.Row="3" 
                Style="{StaticResource Card}"
                Padding="0">
            <Grid KeyboardNavigation.TabNavigation="Local">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Details Header -->
                <Border Grid.Row="0" 
                        Background="{StaticResource SurfaceAltBrush}"
                        BorderBrush="{StaticResource BorderLightBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="24,16">
                    <Grid>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Background="{StaticResource PrimaryBrush}"
                                    Width="32" Height="32"
                                    CornerRadius="6"
                                    Margin="0,0,12,0">
                                <TextBlock Text="{StaticResource Icon.CodeJson}"
                                           Style="{StaticResource Icon}"
                                           Foreground="White"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Finding Details" 
                                           Style="{StaticResource TitleTextStyle}"
                                           FontSize="16"
                                           Margin="0"
                                           AutomationProperties.HeadingLevel="Level2"
                                           AutomationProperties.LiveSetting="Polite"
                                           AutomationProperties.AutomationId="FindingDetailsHeading"/>
                                <TextBlock Text="JSON representation of the selected finding"
                                           Style="{StaticResource CaptionTextStyle}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </StackPanel>
                        
                        <Button Command="{Binding CopyDetailsCommand}"
                                Style="{StaticResource SecondaryButton}"
                                Height="32"
                                Padding="16,6"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                ToolTip="Copy JSON to clipboard">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{StaticResource Icon.Copy}"
                                           Style="{StaticResource Icon}"
                                           Width="14"
                                           Height="14"
                                           FontSize="14"
                                           Margin="0,0,6,0"/>
                                <TextBlock Text="Copy" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </Border>
                           
                <Border Grid.Row="1"
                        Background="{StaticResource SurfaceVariantBrush}"
                        Margin="{StaticResource SpacingM}">
                        <Border Background="{StaticResource SurfaceAltBrush}"
                                CornerRadius="6"
                                BorderThickness="1"
                                BorderBrush="{StaticResource BorderLightBrush}">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                          HorizontalScrollBarVisibility="Auto"
                                          Padding="16">
                                <TextBox Text="{Binding DetailsJson, Mode=OneWay}"
                                         IsReadOnly="True"
                                         TextWrapping="Wrap"
                                         FontFamily="Consolas"
                                         FontSize="12"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="{StaticResource TextPrimaryBrush}"
                                         AutomationProperties.Name="Finding Details JSON"
                                         AutomationProperties.HelpText="JSON representation of the selected finding. Read-only."/>
                            </ScrollViewer>
                        </Border>
                </Border>
            </Grid>
        </Border>
    </Grid>
    </ScrollViewer>
</UserControl>
