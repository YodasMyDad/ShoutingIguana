<UserControl x:Class="ShoutingIguana.Views.FindingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sdk="clr-namespace:ShoutingIguana.PluginSdk;assembly=ShoutingIguana.PluginSdk"
             xmlns:controls="clr-namespace:ShoutingIguana.Controls"
             xmlns:vm="clr-namespace:ShoutingIguana.ViewModels"
             x:Name="Root"
             Background="{StaticResource BackgroundBrush}">
    
    <UserControl.InputBindings>
        <KeyBinding Key="C" Modifiers="Control" Command="{Binding SelectedTab.CopyUrlCommand}"/>
        <KeyBinding Key="C" Modifiers="Control+Shift" Command="{Binding SelectedTab.CopyMessageCommand}"/>
        <KeyBinding Key="Enter" Modifiers="Control" Command="{Binding SelectedTab.OpenInBrowserCommand}"/>
    </UserControl.InputBindings>
    
    <UserControl.Resources>
        <!-- Severity Badge Selector -->
        <Style x:Key="SeverityBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Error}">
                    <Setter Property="Background" Value="{StaticResource ErrorBackgroundBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Warning}">
                    <Setter Property="Background" Value="{StaticResource WarningBackgroundBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Info}">
                    <Setter Property="Background" Value="{StaticResource InfoBackgroundBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="SeverityTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Error}">
                    <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Warning}">
                    <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Severity}" Value="{x:Static sdk:Severity.Info}">
                    <Setter Property="Foreground" Value="{StaticResource InfoBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="{StaticResource SpacingL}" KeyboardNavigation.TabNavigation="Local">
            <Grid.RenderTransform>
                <TranslateTransform/>
            </Grid.RenderTransform>
            <Grid.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                             From="20" To="0" Duration="0:0:0.4"
                                             DecelerationRatio="0.7"/>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0" To="1" Duration="0:0:0.4"
                                             DecelerationRatio="0.7"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Grid.Triggers>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*" MinHeight="300" MaxHeight="600"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*" MinHeight="200"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="8"
                Padding="{StaticResource SpacingL}"
                Margin="0,0,0,24"
                SnapsToDevicePixels="True">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="2" Opacity="0.06"/>
            </Border.Effect>
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource InfoBrush}"
                            Width="44" Height="44"
                            CornerRadius="8"
                            Margin="0,0,16,0">
                        <TextBlock Text="{StaticResource Icon.Search}"
                                   Style="{StaticResource Icon}"
                                   FontSize="22"
                                   Foreground="White"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Findings"
                                   Style="{StaticResource DisplayTextStyle}"
                                   FontSize="24"
                                   Margin="0"
                                   AutomationProperties.HeadingLevel="Level1"
                                   AutomationProperties.LiveSetting="Off"
                                   AutomationProperties.AutomationId="FindingsPageHeading"/>
                        <TextBlock Text="Issues and insights discovered during crawl"
                                   Style="{StaticResource BodyTextStyle}"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel Style="{StaticResource ToolbarPanel}" 
                            AutomationProperties.Name="Findings Toolbar"
                            KeyboardNavigation.TabNavigation="Local">
                    <Button Command="{Binding RefreshCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,6,0"
                            TabIndex="0"
                            AutomationProperties.Name="Refresh Findings"
                            ToolTip="Refresh findings from database (F5)">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{StaticResource Icon.Refresh}"
                                       Style="{StaticResource Icon}"
                                       Width="16" 
                                       Height="16"
                                       Margin="0,0,6,0"/>
                            <TextBlock Text="Refresh" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ExportCommand}"
                            Style="{StaticResource PrimaryButton}"
                            TabIndex="1"
                            AutomationProperties.Name="Export Findings"
                            AutomationProperties.HelpText="Export findings to Excel or CSV with customizable filters"
                            ToolTip="Export findings with customizable format and filters (Ctrl+E)">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{StaticResource Icon.Save}"
                                       Style="{StaticResource Icon}"
                                       Width="16" 
                                       Height="16"
                                       Margin="0,0,6,0"/>
                            <TextBlock Text="Export" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tabbed Findings View -->
        <Border Grid.Row="1" 
                Style="{StaticResource Card}"
                Padding="{StaticResource SpacingXs}">
            <Grid>
                <!-- Empty State (visible when no tabs) -->
                <StackPanel Style="{StaticResource EmptyStateContainer}"
                            Visibility="{Binding Tabs.Count, Converter={StaticResource InverseInt32ToVisibilityConverter}}"
                            x:Name="EmptyStatePanel">
                    <TextBlock Text="{StaticResource Icon.Search}"
                               Style="{StaticResource EmptyStateIcon}"/>
                    <TextBlock Text="No Findings Yet"
                               Style="{StaticResource EmptyStateTitle}"/>
                    <TextBlock Text="Start a crawl to begin analyzing your website. Findings from all enabled plugins will appear here."
                               Style="{StaticResource EmptyStateMessage}"/>
                </StackPanel>
                
                <!-- Tabbed Content (visible when tabs exist) -->
                <TabControl ItemsSource="{Binding Tabs}" 
                            SelectedItem="{Binding SelectedTab}"
                            Style="{StaticResource StandardTabControl}"
                            KeyboardNavigation.TabNavigation="Local"
                            Visibility="{Binding Tabs.Count, Converter={StaticResource Int32ToVisibilityConverter}}">
                <TabControl.ItemContainerStyle>
                    <Style TargetType="TabItem" BasedOn="{StaticResource CompactTabItem}">
                        <Setter Property="MinHeight" Value="32"/>
                    </Style>
                </TabControl.ItemContainerStyle>
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="8,0">
                            <TextBlock Text="{Binding TabHeader}" 
                                       VerticalAlignment="Center"
                                       Margin="0,0,6,0"
                                       FontWeight="Medium"/>
                            <Border Background="{StaticResource ErrorBrush}" 
                                    CornerRadius="8" 
                                    Padding="5,1"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding ErrorCount, Converter={StaticResource Int32ToVisibilityConverter}, FallbackValue=Collapsed}">
                                <TextBlock Text="{Binding ErrorCount}" 
                                           Foreground="White" 
                                           FontSize="9"
                                           FontWeight="Bold"/>
                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.Resources>
                    <!-- DataTemplate for OverviewTabViewModel -->
                    <DataTemplate DataType="{x:Type vm:OverviewTabViewModel}">
                        <Grid Margin="16" MinHeight="350">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*" MinHeight="250" MaxHeight="450"/>
                            </Grid.RowDefinitions>

                            <!-- Description -->
                            <TextBlock Grid.Row="0"
                                       Text="{Binding Description}"
                                       Style="{StaticResource BodyTextStyle}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,0,16"
                                       Visibility="{Binding Description, Converter={StaticResource NullToVisibilityConverter}}"
                                       TextWrapping="Wrap"/>

                            <!-- Search Filter -->
                            <Border Grid.Row="1"
                                    Background="{StaticResource SurfaceAltBrush}"
                                    CornerRadius="6"
                                    Padding="16"
                                    Margin="0,0,0,16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Search Box -->
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Search URLs" 
                                                   Style="{StaticResource FormFieldLabel}"
                                                   Margin="0,0,0,8"/>
                                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource ModernTextBox}"
                                                 ToolTip="Search URLs by address, title, or content type">
                                            <TextBox.Tag>Type to search...</TextBox.Tag>
                                        </TextBox>
                                    </StackPanel>
                                             
                                    <!-- Total Count Badge -->
                                    <Border Grid.Column="2"
                                            Background="{StaticResource PrimaryBrush}"
                                            CornerRadius="8"
                                            Padding="20,12"
                                            VerticalAlignment="Bottom"
                                            Height="40">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="Total: "
                                                       Foreground="White"
                                                       FontWeight="Medium"
                                                       FontSize="13"
                                                       VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding TotalCount, Mode=OneWay}" 
                                                       Foreground="White"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       VerticalAlignment="Center"
                                                       Margin="4,0,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>

                            <!-- URLs DataGrid -->
                            <Grid Grid.Row="2" MinHeight="250">
                                <!-- Skeleton Loader (shown while loading) -->
                                <ItemsControl Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                                              Margin="12">
                                    <ItemsControl.Items>
                                        <!-- 8 skeleton rows -->
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="200"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="280"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="250"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="300"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="180"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="290"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="220"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="310"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="190"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="270"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="240"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="295"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="210"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="285"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                        <Border Style="{StaticResource SkeletonRow}">
                                            <Grid Margin="12,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="110"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="350"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <Rectangle Grid.Column="0" Style="{StaticResource SkeletonCell}" Width="80"/>
                                                <Rectangle Grid.Column="1" Style="{StaticResource SkeletonCell}" Width="230"/>
                                                <Rectangle Grid.Column="2" Style="{StaticResource SkeletonCell}" Width="275"/>
                                                <Rectangle Grid.Column="3" Style="{StaticResource SkeletonCell}" Width="130"/>
                                            </Grid>
                                        </Border>
                                    </ItemsControl.Items>
                                </ItemsControl>
                                
                                <!-- Actual DataGrid (hidden while loading) -->
                                <DataGrid Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                          ItemsSource="{Binding FilteredUrls}"
                                          SelectedItem="{Binding SelectedUrlModel, Mode=TwoWay}"
                                          Style="{StaticResource ModernDataGrid}"
                                          MaxHeight="450"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Auto"
                                          AutomationProperties.Name="URLs List"
                                          AutomationProperties.HelpText="List of all URLs discovered during the crawl. Select a row to view details below.">
                                <DataGrid.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Copy URL" 
                                                  Command="{Binding CopyUrlCommand}">
                                            <MenuItem.Icon>
                                                <TextBlock Text="{StaticResource Icon.Copy}" Style="{StaticResource Icon}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem Header="Open in Browser" 
                                                  Command="{Binding OpenInBrowserCommand}">
                                            <MenuItem.Icon>
                                                <TextBlock Text="{StaticResource Icon.OpenInNew}" Style="{StaticResource Icon}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </DataGrid.ContextMenu>
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="Type" Width="100" MinWidth="90">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border CornerRadius="4" 
                                                        Padding="6,2" 
                                                        HorizontalAlignment="Center">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsInternal}" Value="True">
                                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding IsInternal}" Value="False">
                                                                    <Setter Property="Background" Value="#F5F5F5"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding Type}" 
                                                               FontWeight="Medium"
                                                               FontSize="11">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsInternal}" Value="True">
                                                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsInternal}" Value="False">
                                                                        <Setter Property="Foreground" Value="#757575"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Status" 
                                                        Binding="{Binding Url.HttpStatus}" 
                                                        Width="70"
                                                        MinWidth="60"/>
                                    <DataGridTextColumn Header="Address" 
                                                        Binding="{Binding Url.Address}" 
                                                        Width="3*"
                                                        MinWidth="300"
                                                        FontFamily="Consolas"
                                                        FontSize="12"/>
                                    <DataGridTextColumn Header="Title" 
                                                        Binding="{Binding Url.Title}" 
                                                        Width="2*"
                                                        MinWidth="200"/>
                                    <DataGridTextColumn Header="Content-Type" 
                                                        Binding="{Binding Url.ContentType}" 
                                                        Width="150"
                                                        MinWidth="120"/>
                                    <DataGridTextColumn Header="Size" 
                                                        Binding="{Binding Url.ContentLength, StringFormat='N0'}" 
                                                        Width="100"
                                                        MinWidth="80"/>
                                    <DataGridTextColumn Header="Depth" 
                                                        Binding="{Binding Url.Depth}" 
                                                        Width="70"
                                                        MinWidth="60"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                    
                    <!-- DataTemplate for FindingTabViewModel -->
                    <DataTemplate DataType="{x:Type vm:FindingTabViewModel}">
                        <Grid Margin="16" MinHeight="350">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*" MinHeight="250" MaxHeight="450"/>
                            </Grid.RowDefinitions>

                            <!-- Description -->
                            <TextBlock Grid.Row="0"
                                       Text="{Binding Description}"
                                       Style="{StaticResource BodyTextStyle}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,0,16"
                                       Visibility="{Binding Description, Converter={StaticResource NullToVisibilityConverter}}"
                                       TextWrapping="Wrap"/>

                            <!-- Filters -->
                            <Border Grid.Row="1"
                                    Background="{StaticResource SurfaceAltBrush}"
                                    CornerRadius="6"
                                    Padding="16"
                                    Margin="0,0,0,16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Severity Filter -->
                                    <StackPanel Grid.Column="0" Margin="0">
                                        <TextBlock Text="Severity" 
                                                   Style="{StaticResource FormFieldLabel}"
                                                   Margin="0,0,0,8"/>
                                        <ComboBox Width="180"
                                                  MinWidth="160"
                                                  MaxDropDownHeight="300"
                                                  SelectedValue="{Binding SelectedSeverity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource ModernComboBox}"
                                                  ToolTip="Filter findings by severity level">
                                            <ComboBoxItem Tag="{x:Null}" MaxWidth="180">
                                                <TextBlock Text="All Severities"/>
                                            </ComboBoxItem>
                                            <ComboBoxItem Tag="{x:Static sdk:Severity.Error}" MaxWidth="180">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{StaticResource Icon.Error}" 
                                                               Style="{StaticResource Icon}"
                                                               FontSize="14"
                                                               Foreground="{StaticResource ErrorBrush}"
                                                               Margin="0,0,8,0"/>
                                                    <TextBlock Text="Error" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </ComboBoxItem>
                                            <ComboBoxItem Tag="{x:Static sdk:Severity.Warning}" MaxWidth="180">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{StaticResource Icon.Warning}" 
                                                               Style="{StaticResource Icon}"
                                                               FontSize="14"
                                                               Foreground="{StaticResource WarningBrush}"
                                                               Margin="0,0,8,0"/>
                                                    <TextBlock Text="Warning" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </ComboBoxItem>
                                            <ComboBoxItem Tag="{x:Static sdk:Severity.Info}" MaxWidth="180">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{StaticResource Icon.Info}" 
                                                               Style="{StaticResource Icon}"
                                                               FontSize="14"
                                                               Foreground="{StaticResource InfoBrush}"
                                                               Margin="0,0,8,0"/>
                                                    <TextBlock Text="Info" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </ComboBoxItem>
                                        </ComboBox>
                                    </StackPanel>
                                    
                                    <!-- Search Box -->
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Search" 
                                                   Style="{StaticResource FormFieldLabel}"
                                                   Margin="0,0,0,8"/>
                                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource ModernTextBox}"
                                                 ToolTip="Search findings by message, code, or URL">
                                            <TextBox.Tag>Type to search...</TextBox.Tag>
                                        </TextBox>
                                    </StackPanel>
                                             
                                    <!-- Total Count Badge -->
                                    <Border Grid.Column="4"
                                            Background="{StaticResource PrimaryBrush}"
                                            CornerRadius="8"
                                            Padding="20,12"
                                            VerticalAlignment="Bottom"
                                            Height="40">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="Total: "
                                                       Foreground="White"
                                                       FontWeight="Medium"
                                                       FontSize="13"
                                                       VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding TotalCount, Mode=OneWay}" 
                                                       Foreground="White"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       VerticalAlignment="Center"
                                                       Margin="4,0,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>

                            <!-- Findings DataGrid -->
                            <DataGrid Grid.Row="2" 
                                      Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                      ItemsSource="{Binding FilteredFindings}"
                                      SelectedItem="{Binding SelectedFinding, Mode=TwoWay}"
                                      Style="{StaticResource ModernDataGrid}"
                                      MaxHeight="450"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Auto"
                                      AutomationProperties.Name="Findings List">
                            <DataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Copy URL" 
                                              Command="{Binding CopyUrlCommand}">
                                        <MenuItem.Icon>
                                            <TextBlock Text="{StaticResource Icon.Copy}" Style="{StaticResource Icon}"/>
                                        </MenuItem.Icon>
                                    </MenuItem>
                                    <MenuItem Header="Copy Message" 
                                              Command="{Binding CopyMessageCommand}">
                                        <MenuItem.Icon>
                                            <TextBlock Text="{StaticResource Icon.Copy}" Style="{StaticResource Icon}"/>
                                        </MenuItem.Icon>
                                    </MenuItem>
                                    <Separator/>
                                    <MenuItem Header="Open in Browser" 
                                              Command="{Binding OpenInBrowserCommand}">
                                        <MenuItem.Icon>
                                            <TextBlock Text="{StaticResource Icon.OpenInNew}" Style="{StaticResource Icon}"/>
                                        </MenuItem.Icon>
                                    </MenuItem>
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Severity" Width="120" MinWidth="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource SeverityBadgeStyle}">
                                                <TextBlock Text="{Binding Severity}" 
                                                           Style="{StaticResource SeverityTextStyle}"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Message" 
                                                    Binding="{Binding Message}" 
                                                    Width="2*"
                                                    MinWidth="200"/>
                                <DataGridTextColumn Header="URL" 
                                                    Binding="{Binding Url.Address}" 
                                                    Width="3*"
                                                    MinWidth="250"
                                                    FontFamily="Consolas"
                                                    FontSize="12"/>
                                <DataGridTextColumn Header="Date" 
                                                    Binding="{Binding CreatedUtc, StringFormat='yyyy-MM-dd HH:mm:ss'}" 
                                                    Width="150"
                                                    MinWidth="140"
                                                    FontFamily="Consolas"
                                                    FontSize="12"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </DataTemplate>
                </TabControl.Resources>
                </TabControl>
            </Grid>
        </Border>

        <!-- Splitter -->
        <GridSplitter Grid.Row="2" 
                      Height="8" 
                      HorizontalAlignment="Stretch" 
                      Style="{StaticResource ModernGridSplitter}"
                      Cursor="SizeNS"
                      Margin="0,8"
                      AutomationProperties.Name="Resize Findings and Details Panels"/>

        <!-- Details Panel -->
        <Border Grid.Row="3" 
                Style="{StaticResource Card}"
                Padding="0">
            <Grid KeyboardNavigation.TabNavigation="Local">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Details Header -->
                <Border Grid.Row="0" 
                        Background="{StaticResource SurfaceAltBrush}"
                        BorderBrush="{StaticResource BorderLightBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="{StaticResource SpacingM}">
                    <Grid>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Background="{StaticResource PrimaryBrush}"
                                    Width="32" Height="32"
                                    CornerRadius="6"
                                    Margin="0,0,12,0">
                                <TextBlock Text="{StaticResource Icon.Info}"
                                           Style="{StaticResource Icon}"
                                           Foreground="White"/>
                            </Border>
                            <TextBlock Text="{Binding DetailsHeaderText}" 
                                       Style="{StaticResource TitleTextStyle}"
                                       FontSize="16"
                                       Margin="0"
                                       VerticalAlignment="Center"
                                       AutomationProperties.HeadingLevel="Level2"
                                       AutomationProperties.LiveSetting="Polite"
                                       AutomationProperties.AutomationId="DetailsHeading"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" 
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center">
                            <CheckBox IsChecked="{Binding IsTechnicalModeEnabled}"
                                      Content="Technical"
                                      Margin="0,0,12,0"
                                      VerticalAlignment="Center"
                                      Visibility="{Binding IsFindingTabSelected, Converter={StaticResource BooleanToVisibilityConverter}}"
                                      AutomationProperties.Name="Technical Mode"
                                      AutomationProperties.HelpText="Toggle to view technical metadata JSON"
                                      ToolTip="Show technical metadata JSON"/>
                            <Button Command="{Binding CopyDetailsCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Height="32"
                                    Padding="16,6"
                                    VerticalAlignment="Center"
                                    AutomationProperties.Name="Copy Details"
                                    AutomationProperties.HelpText="Copies the current view content to the clipboard"
                                    ToolTip="Copy to clipboard">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{StaticResource Icon.Copy}"
                                               Style="{StaticResource Icon}"
                                               Width="14"
                                               Height="14"
                                               FontSize="14"
                                               Margin="0,0,6,0"/>
                                    <TextBlock Text="Copy" VerticalAlignment="Center" FontSize="13"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
                           
                <Border Grid.Row="1"
                        Background="{StaticResource SurfaceVariantBrush}"
                        Margin="{StaticResource SpacingM}">
                    <Border Background="{StaticResource SurfaceAltBrush}"
                            CornerRadius="6"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BorderLightBrush}">
                        <ContentControl Content="{Binding SelectedTab}">
                            <ContentControl.Resources>
                                <!-- DataTemplate for OverviewTabViewModel - Show URL Properties -->
                                <DataTemplate DataType="{x:Type vm:OverviewTabViewModel}">
                                    <DataGrid ItemsSource="{Binding UrlProperties}"
                                              AutoGenerateColumns="False"
                                              IsReadOnly="True"
                                              CanUserAddRows="False"
                                              CanUserDeleteRows="False"
                                              CanUserResizeRows="False"
                                              CanUserSortColumns="False"
                                              GridLinesVisibility="None"
                                              HeadersVisibility="None"
                                              Background="{StaticResource SurfaceBrush}"
                                              BorderThickness="0"
                                              FontSize="14"
                                              ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                              Margin="{StaticResource SpacingM}"
                                              Loaded="UrlPropertiesDataGrid_Loaded"
                                              PreviewMouseWheel="DataGrid_PreviewMouseWheel"
                                              IsHitTestVisible="True"
                                              Focusable="False">
                                        <DataGrid.Resources>
                                            <!-- Override system selection colors to be transparent -->
                                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent"/>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#1F1F1F"/>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent"/>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#1F1F1F"/>
                                        </DataGrid.Resources>
                                        <DataGrid.RowStyle>
                                            <Style TargetType="DataGridRow">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Cursor" Value="Arrow"/>
                                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                                <Setter Property="MinHeight" Value="36"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="DataGridRow">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    BorderBrush="{StaticResource BorderLightBrush}"
                                                                    BorderThickness="0,0,0,1"
                                                                    SnapsToDevicePixels="True">
                                                                <SelectiveScrollingGrid>
                                                                    <SelectiveScrollingGrid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                        <ColumnDefinition/>
                                                                    </SelectiveScrollingGrid.ColumnDefinitions>
                                                                    <SelectiveScrollingGrid.RowDefinitions>
                                                                        <RowDefinition Height="Auto"/>
                                                                        <RowDefinition Height="Auto"/>
                                                                    </SelectiveScrollingGrid.RowDefinitions>
                                                                    <DataGridCellsPresenter Grid.Column="1"
                                                                                            ItemsPanel="{TemplateBinding ItemsPanel}"
                                                                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                                    <DataGridDetailsPresenter Grid.Column="1"
                                                                                              Grid.Row="1"
                                                                                              SelectiveScrollingGrid.SelectiveScrollingOrientation="{Binding AreRowDetailsFrozen, ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}, Converter={x:Static DataGrid.RowDetailsScrollingConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                                                              Visibility="{TemplateBinding DetailsVisibility}"/>
                                                                    <DataGridRowHeader Grid.RowSpan="2"
                                                                                       SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                                                                                       Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Row}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>
                                                                </SelectiveScrollingGrid>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </DataGrid.RowStyle>
                                        <DataGrid.CellStyle>
                                            <Style TargetType="DataGridCell">
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                                <Setter Property="Padding" Value="16,8"/>
                                                <Setter Property="VerticalContentAlignment" Value="Top"/>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                                <Setter Property="Focusable" Value="False"/>
                                                <Setter Property="IsTabStop" Value="False"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="DataGridCell">
                                                            <Border Padding="{TemplateBinding Padding}"
                                                                    Background="Transparent"
                                                                    SnapsToDevicePixels="True">
                                                                <ContentPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGrid.CellStyle>
                                        <DataGrid.Columns>
                                            <DataGridTemplateColumn Header="Property" Width="250">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding Key}" 
                                                                   Style="{StaticResource BodyEmphasisTextStyle}"
                                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                                   VerticalAlignment="Center"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Header="Value" Width="*">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBox Text="{Binding Value, Mode=OneWay}" 
                                                                 IsReadOnly="True"
                                                                 TextWrapping="Wrap"
                                                                 AcceptsReturn="True"
                                                                 BorderThickness="0"
                                                                 Background="Transparent"
                                                                 Foreground="{StaticResource TextPrimaryBrush}"
                                                                 FontFamily="{StaticResource BodyFontFamily}"
                                                                 FontSize="14"
                                                                 Padding="0"
                                                                 Margin="0"
                                                                 IsTabStop="False"
                                                                 Focusable="False"
                                                                 IsReadOnlyCaretVisible="False"
                                                                 Cursor="Arrow">
                                                            <TextBox.Style>
                                                                <Style TargetType="TextBox">
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="TextBox">
                                                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                                                              Focusable="False"
                                                                                              HorizontalScrollBarVisibility="Disabled"
                                                                                              VerticalScrollBarVisibility="Disabled"/>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                </Style>
                                                            </TextBox.Style>
                                                        </TextBox>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                        <DataGrid.GroupStyle>
                                            <GroupStyle>
                                                <GroupStyle.HeaderTemplate>
                                                    <DataTemplate>
                                                        <Border Background="{StaticResource PrimaryBrush}" 
                                                                Padding="{StaticResource SpacingS}"
                                                                Margin="0,8,0,0"
                                                                CornerRadius="4"
                                                                SnapsToDevicePixels="True">
                                                            <TextBlock Text="{Binding Name}" 
                                                                       Style="{StaticResource BodyEmphasisTextStyle}"
                                                                       Foreground="White"
                                                                       FontSize="14"/>
                                                        </Border>
                                                    </DataTemplate>
                                                </GroupStyle.HeaderTemplate>
                                            </GroupStyle>
                                        </DataGrid.GroupStyle>
                                    </DataGrid>
                                </DataTemplate>
                                
                                <!-- DataTemplate for FindingTabViewModel - Show Finding Details -->
                                <DataTemplate DataType="{x:Type vm:FindingTabViewModel}">
                                    <Grid>
                                        <!-- Normal View: Formatted Text from Plugin -->
                                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                      HorizontalScrollBarVisibility="Disabled"
                                                      Padding="16"
                                                      Visibility="{Binding DataContext.IsTechnicalModeEnabled, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                            <StackPanel>
                                                <!-- Show structured details if available -->
                                                <controls:FindingDetailsViewer 
                                                    Details="{Binding SelectedFindingDetails}"
                                                    Visibility="{Binding SelectedFindingDetails, Converter={StaticResource NullToVisibilityConverter}}"/>
                                                
                                                <!-- Show message if no structured details -->
                                                <TextBlock Text="Select a finding to view details."
                                                           Style="{StaticResource BodyTextStyle}"
                                                           Foreground="{StaticResource TextSecondaryBrush}"
                                                           Visibility="{Binding SelectedFindingDetails, Converter={StaticResource InverseNullToVisibilityConverter}}"
                                                           Margin="0,8"/>
                                            </StackPanel>
                                        </ScrollViewer>
                                        
                                        <!-- Technical View: JSON Technical Metadata -->
                                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                      HorizontalScrollBarVisibility="Auto"
                                                      Padding="16"
                                                      Visibility="{Binding DataContext.IsTechnicalModeEnabled, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <StackPanel>
                                                <!-- Show technical metadata if available -->
                                                <TextBox Text="{Binding TechnicalMetadataJson, Mode=OneWay}"
                                                         IsReadOnly="True"
                                                         TextWrapping="Wrap"
                                                         FontFamily="Consolas"
                                                         FontSize="12"
                                                         Background="Transparent"
                                                         BorderThickness="0"
                                                         Foreground="{StaticResource TextPrimaryBrush}"
                                                         Visibility="{Binding HasTechnicalMetadata, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                
                                                <!-- Show message if no technical metadata -->
                                                <TextBlock Text="No technical metadata available."
                                                           Style="{StaticResource BodyTextStyle}"
                                                           Foreground="{StaticResource TextSecondaryBrush}"
                                                           Visibility="{Binding HasTechnicalMetadata, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                                           Margin="0,8"/>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </Grid>
                                </DataTemplate>
                            </ContentControl.Resources>
                        </ContentControl>
                    </Border>
                </Border>
            </Grid>
        </Border>
    </Grid>
    </ScrollViewer>
</UserControl>
