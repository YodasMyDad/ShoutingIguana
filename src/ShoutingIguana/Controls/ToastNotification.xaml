<UserControl x:Class="ShoutingIguana.Controls.ToastNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:services="clr-namespace:ShoutingIguana.Services">
    
    <UserControl.Resources>
        <!-- Toast background colors based on type -->
        <SolidColorBrush x:Key="SuccessBackground" Color="#E8F5E9"/>
        <SolidColorBrush x:Key="ErrorBackground" Color="#FFEBEE"/>
        <SolidColorBrush x:Key="InfoBackground" Color="#E3F2FD"/>
        <SolidColorBrush x:Key="WarningBackground" Color="#FFF8E1"/>
        
        <!-- Exit animation storyboard -->
        <Storyboard x:Key="ExitStoryboard">
            <DoubleAnimation Storyboard.TargetName="SlideTransform"
                             Storyboard.TargetProperty="X"
                             To="400" 
                             Duration="0:0:0.25">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             To="0" 
                             Duration="0:0:0.25"/>
        </Storyboard>
    </UserControl.Resources>
    
    <UserControl.RenderTransform>
        <TranslateTransform x:Name="SlideTransform"/>
    </UserControl.RenderTransform>
    
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="SlideTransform"
                                     Storyboard.TargetProperty="X"
                                     From="400" To="0" 
                                     Duration="0:0:0.3">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" 
                                     Duration="0:0:0.3"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
    
    <Border x:Name="ToastBorder"
            CornerRadius="8"
            Padding="16,12"
            MinWidth="300"
            MaxWidth="400"
            Margin="0,0,0,12"
            BorderThickness="0,0,0,3"
            SnapsToDevicePixels="True">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="{StaticResource InfoBackground}"/>
                <Setter Property="BorderBrush" Value="{StaticResource InfoBrush}"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Type}" Value="{x:Static services:ToastType.Success}">
                        <Setter Property="Background" Value="{StaticResource SuccessBackground}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource SuccessBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Type}" Value="{x:Static services:ToastType.Error}">
                        <Setter Property="Background" Value="{StaticResource ErrorBackground}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Type}" Value="{x:Static services:ToastType.Warning}">
                        <Setter Property="Background" Value="{StaticResource WarningBackground}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="4" Opacity="0.15"/>
        </Border.Effect>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Icon -->
            <Border Grid.Column="0" 
                    Width="32" 
                    Height="32"
                    CornerRadius="16"
                    Margin="0,0,12,0"
                    VerticalAlignment="Top">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource InfoBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Type}" Value="{x:Static services:ToastType.Success}">
                                <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="{x:Static services:ToastType.Error}">
                                <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="{x:Static services:ToastType.Warning}">
                                <Setter Property="Background" Value="{StaticResource WarningBrush}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                
                <TextBlock Text="{Binding Icon}" 
                           Style="{StaticResource Icon}"
                           FontSize="16"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
            
            <!-- Message -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}" 
                           Style="{StaticResource BodyEmphasisTextStyle}"
                           TextWrapping="Wrap"/>
                <TextBlock Text="{Binding Message}" 
                           Style="{StaticResource CaptionTextStyle}"
                           TextWrapping="Wrap"
                           Visibility="{Binding Message, Converter={StaticResource NullToVisibilityConverter}}"
                           Margin="0,4,0,0"/>
            </StackPanel>
            
            <!-- Close button -->
            <Button Grid.Column="2" 
                    Command="{Binding CloseCommand}"
                    Width="28" 
                    Height="28"
                    Padding="0"
                    Margin="8,0,0,0"
                    VerticalAlignment="Top"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    ToolTip="Close">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="14"
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#20000000"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#30000000"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <TextBlock Text="{StaticResource Icon.Close}" 
                           Style="{StaticResource Icon}"
                           FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
            </Button>
        </Grid>
    </Border>
</UserControl>

